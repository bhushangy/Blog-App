<% include partials/header %>


<div class="ui main text container segment">
    <div class="ui huge header">New Post</div>
    <form class="ui form" action="/blogs" method="POST">
        <div class="field">
            <label>Title</label>
            <input type="text" name="blog[title]"><!--see what u r doin here is creting an object and pushing all the contents of the form into that object...so u can pass it as single object into database instead of creating an object urself by extracting all params..-->
        </div>
        <div class="field">
            <label>Image</label>
            <input type="text" name="blog[image]">
        </div>
        <div class="field">
            <label>Post Content</label>
            <textarea name="blog[body]" required></textarea>
        </div>
        <button class="ui big violet basic button">Create Post</button>
    </form>
</div>


<% include partials/footer %>